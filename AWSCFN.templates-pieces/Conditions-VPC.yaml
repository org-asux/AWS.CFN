  # AlwaysTrue${ASUX::foreach.index+1}: !Not  ## This condition is GUARANTEED to be true, as 'MyStackNamePrefix' is a required parameter and if it's NULL the CFN-template will fail to execute anyway
  #   - !Equals
  #     - ''
  #     - !Ref "MyVPCStackPrefix"


#EOF
